from setuptools import setup

version = '0.1'

setup(
    name='zipencrypt',
    version=version,
    packages=['zipencrypt'],
    classifiers=(
        'Programming Language :: Python',
        'Programming Language :: Python :: 2.7',
    )
)
